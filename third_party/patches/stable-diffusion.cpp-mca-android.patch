diff --git a/include/stable-diffusion.h b/include/stable-diffusion.h
index e15f1aa..a98bc6d 100644
--- a/include/stable-diffusion.h
+++ b/include/stable-diffusion.h
@@ -399,17 +399,106 @@ typedef struct {

 typedef struct sd_ctx_t sd_ctx_t;

+enum {
+    SD_IMAGE_EXECUTION_EVIDENCE_VERSION = 4,
+};
+
+typedef struct {
+    uint32_t version;
+    uint32_t positive_conditioning_observed;
+    uint32_t negative_conditioning_observed;
+    uint32_t positive_prompt_weighting_capable;
+    uint32_t negative_prompt_weighting_capable;
+    uint32_t prompt_weighting_applied;
+    uint32_t generation_completed;
+    uint32_t flow_shift_applied;
+    uint32_t dynamic_flow_shift;
+    uint32_t distilled_guidance_applied;
+    float effective_flow_shift;
+    float effective_distilled_guidance;
+    uint64_t positive_conditioning_token_count;
+    uint64_t negative_conditioning_token_count;
+    uint64_t positive_non_unity_weight_count;
+    uint64_t negative_non_unity_weight_count;
+    uint64_t completed_sampling_step_count;
+    uint64_t sampling_pass_count;
+    uint64_t diffusion_model_compute_count;
+    uint64_t positive_diffusion_model_compute_count;
+    uint64_t negative_diffusion_model_compute_count;
+    uint64_t auxiliary_diffusion_model_compute_count;
+    uint64_t conditioning_artifact_byte_count;
+    uint32_t lora_requested_count;
+    uint32_t lora_loaded_count;
+    uint32_t lora_applied_count;
+    uint64_t lora_applied_tensor_count;
+    uint64_t control_net_compute_attempt_count;
+    uint64_t control_net_compute_success_count;
+    uint64_t positive_control_net_compute_attempt_count;
+    uint64_t positive_control_net_compute_success_count;
+    uint64_t negative_control_net_compute_attempt_count;
+    uint64_t negative_control_net_compute_success_count;
+    uint64_t control_net_residual_consumption_count;
+    uint64_t positive_control_net_residual_consumption_count;
+    uint64_t negative_control_net_residual_consumption_count;
+    uint64_t auxiliary_control_net_residual_consumption_count;
+    uint64_t vae_encode_tiling_invocation_count;
+    uint64_t vae_encode_tiling_success_count;
+    uint64_t vae_decode_tiling_invocation_count;
+    uint64_t vae_decode_tiling_success_count;
+    uint64_t vae_encode_planned_tile_count;
+    uint64_t vae_decode_planned_tile_count;
+    uint64_t vae_encode_tile_compute_attempt_count;
+    uint64_t vae_encode_tile_compute_success_count;
+    uint64_t vae_decode_tile_compute_attempt_count;
+    uint64_t vae_decode_tile_compute_success_count;
+    uint32_t vae_encode_tile_size_x;
+    uint32_t vae_encode_tile_size_y;
+    uint32_t vae_decode_tile_size_x;
+    uint32_t vae_decode_tile_size_y;
+    float vae_encode_tile_overlap_x;
+    float vae_encode_tile_overlap_y;
+    float vae_decode_tile_overlap_x;
+    float vae_decode_tile_overlap_y;
+} sd_image_execution_evidence_t;
+
+#ifdef __cplusplus
+static_assert(SD_IMAGE_EXECUTION_EVIDENCE_VERSION == 4u,
+              "image execution evidence ABI version changed");
+static_assert(sizeof(sd_image_execution_evidence_t) == 352u,
+              "image execution evidence ABI size changed");
+static_assert(offsetof(sd_image_execution_evidence_t, version) == 0u,
+              "image execution evidence version offset changed");
+static_assert(offsetof(sd_image_execution_evidence_t, control_net_compute_attempt_count) == 160u,
+              "image execution evidence ControlNet offset changed");
+static_assert(offsetof(sd_image_execution_evidence_t, auxiliary_control_net_residual_consumption_count) == 232u,
+              "image execution evidence ControlNet tail offset changed");
+static_assert(offsetof(sd_image_execution_evidence_t, vae_encode_tiling_invocation_count) == 240u,
+              "image execution evidence VAE offset changed");
+static_assert(offsetof(sd_image_execution_evidence_t, vae_decode_tile_overlap_y) == 348u,
+              "image execution evidence VAE tail offset changed");
+#endif
+
 typedef void (*sd_log_cb_t)(enum sd_log_level_t level, const char* text, void* data);
 typedef void (*sd_progress_cb_t)(int step, int steps, float time, void* data);
+typedef bool (*sd_cancel_cb_t)(void* data);
 typedef void (*sd_preview_cb_t)(int step, int frame_count, sd_image_t* frames, bool is_noisy, void* data);

 SD_API void sd_set_log_callback(sd_log_cb_t sd_log_cb, void* data);
 SD_API void sd_set_progress_callback(sd_progress_cb_t cb, void* data);
+SD_API void sd_set_cancel_callback(sd_cancel_cb_t cb, void* data);
 SD_API void sd_set_preview_callback(sd_preview_cb_t cb, enum preview_t mode, int interval, bool denoised, bool noisy, void* data);
 SD_API int32_t sd_get_num_physical_cores();
 SD_API const char* sd_get_system_info();
 SD_API bool sd_ctx_supports_image_generation(const sd_ctx_t* sd_ctx);
 SD_API bool sd_ctx_supports_video_generation(const sd_ctx_t* sd_ctx);
+SD_API bool sd_ctx_uses_flow_shift(const sd_ctx_t* sd_ctx);
+SD_API bool sd_ctx_uses_dynamic_flow_shift(const sd_ctx_t* sd_ctx);
+SD_API bool sd_ctx_uses_distilled_guidance(const sd_ctx_t* sd_ctx);
+SD_API bool sd_get_last_image_execution_evidence(const sd_ctx_t* sd_ctx,
+                                                  sd_image_execution_evidence_t* evidence);
+SD_API bool sd_copy_last_image_conditioning_artifact(const sd_ctx_t* sd_ctx,
+                                                      uint8_t* destination,
+                                                      size_t destination_size);

 SD_API const char* sd_type_name(enum sd_type_t type);
 SD_API enum sd_type_t str_to_sd_type(const char* str);
@@ -457,6 +546,36 @@ SD_API bool generate_video(sd_ctx_t* sd_ctx,

 typedef struct upscaler_ctx_t upscaler_ctx_t;

+#define SD_UPSCALER_EXECUTION_EVIDENCE_VERSION 1u
+
+typedef struct sd_upscaler_execution_evidence_t {
+    uint32_t version;
+    uint32_t requested_scale;
+    uint32_t native_scale;
+    uint32_t source_width;
+    uint32_t source_height;
+    uint32_t output_width;
+    uint32_t output_height;
+    uint32_t tiled;
+    uint32_t completed;
+    uint32_t cancelled;
+    uint64_t compute_invocation_count;
+    uint64_t compute_success_count;
+    uint64_t tile_compute_invocation_count;
+    uint64_t tile_compute_success_count;
+} sd_upscaler_execution_evidence_t;
+
+#ifdef __cplusplus
+static_assert(sizeof(sd_upscaler_execution_evidence_t) == 72u,
+              "upscaler execution evidence ABI size changed");
+static_assert(offsetof(sd_upscaler_execution_evidence_t, version) == 0u,
+              "upscaler execution evidence version offset changed");
+static_assert(offsetof(sd_upscaler_execution_evidence_t, compute_invocation_count) == 40u,
+              "upscaler execution evidence compute offset changed");
+static_assert(offsetof(sd_upscaler_execution_evidence_t, tile_compute_success_count) == 64u,
+              "upscaler execution evidence tile offset changed");
+#endif
+
 SD_API upscaler_ctx_t* new_upscaler_ctx(const char* esrgan_path,
                                         bool offload_params_to_cpu,
                                         bool direct,
@@ -471,6 +590,9 @@ SD_API sd_image_t upscale(upscaler_ctx_t* upscaler_ctx,
                           uint32_t upscale_factor);

 SD_API int get_upscale_factor(upscaler_ctx_t* upscaler_ctx);
+SD_API bool sd_get_upscaler_execution_evidence(
+    const upscaler_ctx_t* upscaler_ctx,
+    sd_upscaler_execution_evidence_t* evidence);

 SD_API bool convert(const char* input_path,
                     const char* vae_path,
diff --git a/src/conditioner.hpp b/src/conditioner.hpp
index bb4e1c2..37e33d7 100644
--- a/src/conditioner.hpp
+++ b/src/conditioner.hpp
@@ -1,7 +1,9 @@
 #ifndef __CONDITIONER_HPP__
 #define __CONDITIONER_HPP__

+#include <algorithm>
 #include <cmath>
+#include <cstdint>
 #include <limits>
 #include <optional>

@@ -62,6 +64,81 @@ struct SDCondition {
     }
 };

+struct ConditioningExecutionSequenceEvidence {
+    std::vector<int32_t> token_ids;
+    std::vector<float> token_weights;
+    bool weights_applied = false;
+    bool counts_toward_token_totals = true;
+};
+
+struct ConditioningExecutionEvidence {
+    std::vector<ConditioningExecutionSequenceEvidence> sequences;
+
+    bool observed() const {
+        return !sequences.empty();
+    }
+
+    bool prompt_weighting_capable() const {
+        return observed() &&
+               std::all_of(sequences.begin(),
+                           sequences.end(),
+                           [](const ConditioningExecutionSequenceEvidence& sequence) {
+                               return sequence.weights_applied;
+                           });
+    }
+};
+
+static inline void record_conditioning_execution_sequence(
+    ConditioningExecutionEvidence* evidence,
+    const std::vector<int>& tokens,
+    const std::vector<float>& weights,
+    bool weights_applied,
+    bool counts_toward_token_totals = true) {
+    if (evidence == nullptr || tokens.empty() || tokens.size() != weights.size()) {
+        return;
+    }
+    ConditioningExecutionSequenceEvidence sequence;
+    sequence.token_ids.assign(tokens.begin(), tokens.end());
+    sequence.token_weights = weights;
+    sequence.weights_applied = weights_applied;
+    sequence.counts_toward_token_totals = counts_toward_token_totals;
+    evidence->sequences.push_back(std::move(sequence));
+}
+
+static inline void record_conditioning_execution_sequences(
+    ConditioningExecutionEvidence* evidence,
+    const std::vector<std::pair<std::vector<int>, std::vector<float>>>& sequences,
+    bool weights_applied) {
+    size_t counted_sequence = sequences.size();
+    bool counted_sequence_has_non_unity_weights = false;
+    for (size_t index = 0; index < sequences.size(); ++index) {
+        const auto& tokens = sequences[index].first;
+        const auto& weights = sequences[index].second;
+        if (tokens.empty() || tokens.size() != weights.size()) {
+            continue;
+        }
+        const bool has_non_unity_weights = std::any_of(
+            weights.begin(),
+            weights.end(),
+            [](float weight) { return weight != 1.0f; });
+        if (counted_sequence == sequences.size() ||
+            (has_non_unity_weights && !counted_sequence_has_non_unity_weights) ||
+            (has_non_unity_weights == counted_sequence_has_non_unity_weights &&
+             tokens.size() > sequences[counted_sequence].first.size())) {
+            counted_sequence = index;
+            counted_sequence_has_non_unity_weights = has_non_unity_weights;
+        }
+    }
+    for (size_t index = 0; index < sequences.size(); ++index) {
+        const auto& sequence = sequences[index];
+        record_conditioning_execution_sequence(evidence,
+                                               sequence.first,
+                                               sequence.second,
+                                               weights_applied,
+                                               index == counted_sequence);
+    }
+}
+
 static inline sd::Tensor<float> apply_token_weights(sd::Tensor<float> hidden_states,
                                                     const std::vector<float>& weights) {
     if (hidden_states.empty()) {
@@ -105,6 +182,7 @@ struct ConditionerParams {
     bool zero_out_masked                             = false;
     int num_input_imgs                               = 0;        // for photomaker
     const std::vector<sd::Tensor<float>>* ref_images = nullptr;  // for qwen image edit
+    ConditioningExecutionEvidence* execution_evidence = nullptr;
 };

 struct Conditioner {
@@ -506,7 +584,8 @@ struct FrozenCLIPEmbedderWithCustomWords : public Conditioner {
                                              int clip_skip,
                                              int width,
                                              int height,
-                                             bool zero_out_masked = false) {
+                                             bool zero_out_masked = false,
+                                             ConditioningExecutionEvidence* execution_evidence = nullptr) {
         int64_t t0 = ggml_time_ms();
         sd::Tensor<float> hidden_states;  // [n_token, hidden_size] or [n_token, hidden_size + hidden_size2]
         sd::Tensor<float> pooled;
@@ -620,6 +699,24 @@ struct FrozenCLIPEmbedderWithCustomWords : public Conditioner {
         if (!vec.empty()) {
             result.c_vector = std::move(vec);
         }
+        record_conditioning_execution_sequence(execution_evidence, tokens, weights, true, true);
+        if (sd_version_is_sdxl(version)) {
+            std::vector<int> secondary_tokens = tokens;
+            for (size_t offset = 0; offset < secondary_tokens.size(); offset += chunk_len) {
+                auto chunk_begin = secondary_tokens.begin() + offset;
+                auto chunk_end = secondary_tokens.begin() +
+                                 std::min(secondary_tokens.size(), offset + chunk_len);
+                auto eos = std::find(chunk_begin, chunk_end, tokenizer.EOS_TOKEN_ID);
+                if (eos != chunk_end) {
+                    std::fill(std::next(eos), chunk_end, 0);
+                }
+            }
+            record_conditioning_execution_sequence(execution_evidence,
+                                                   secondary_tokens,
+                                                   weights,
+                                                   true,
+                                                   false);
+        }
         return result;
     }

@@ -651,7 +748,8 @@ struct FrozenCLIPEmbedderWithCustomWords : public Conditioner {
                                                  conditioner_params.clip_skip,
                                                  conditioner_params.width,
                                                  conditioner_params.height,
-                                                 conditioner_params.zero_out_masked);
+                                                 conditioner_params.zero_out_masked,
+                                                 conditioner_params.execution_evidence);
         return std::make_tuple(cond, clsm);
     }

@@ -677,7 +775,8 @@ struct FrozenCLIPEmbedderWithCustomWords : public Conditioner {
                                             conditioner_params.clip_skip,
                                             conditioner_params.width,
                                             conditioner_params.height,
-                                            conditioner_params.zero_out_masked);
+                                            conditioner_params.zero_out_masked,
+                                            conditioner_params.execution_evidence);
     }
 };

@@ -944,7 +1043,8 @@ struct SD3CLIPEmbedder : public Conditioner {
     SDCondition get_learned_condition_common(int n_threads,
                                              std::vector<std::pair<std::vector<int>, std::vector<float>>> token_and_weights,
                                              int clip_skip,
-                                             bool zero_out_masked = false) {
+                                             bool zero_out_masked = false,
+                                             ConditioningExecutionEvidence* execution_evidence = nullptr) {
         auto& clip_l_tokens  = token_and_weights[0].first;
         auto& clip_l_weights = token_and_weights[0].second;
         auto& clip_g_tokens  = token_and_weights[1].first;
@@ -1098,6 +1198,7 @@ struct SD3CLIPEmbedder : public Conditioner {
         SDCondition result;
         result.c_crossattn = std::move(hidden_states);
         result.c_vector    = std::move(pooled);
+        record_conditioning_execution_sequences(execution_evidence, token_and_weights, true);
         return result;
     }

@@ -1107,7 +1208,8 @@ struct SD3CLIPEmbedder : public Conditioner {
         return get_learned_condition_common(n_threads,
                                             tokens_and_weights,
                                             conditioner_params.clip_skip,
-                                            conditioner_params.zero_out_masked);
+                                            conditioner_params.zero_out_masked,
+                                            conditioner_params.execution_evidence);
     }
 };

@@ -1279,7 +1381,8 @@ struct FluxCLIPEmbedder : public Conditioner {
     SDCondition get_learned_condition_common(int n_threads,
                                              std::vector<std::pair<std::vector<int>, std::vector<float>>> token_and_weights,
                                              int clip_skip,
-                                             bool zero_out_masked = false) {
+                                             bool zero_out_masked = false,
+                                             ConditioningExecutionEvidence* execution_evidence = nullptr) {
         auto& clip_l_tokens  = token_and_weights[0].first;
         auto& clip_l_weights = token_and_weights[0].second;
         auto& t5_tokens      = token_and_weights[1].first;
@@ -1356,6 +1459,7 @@ struct FluxCLIPEmbedder : public Conditioner {
         SDCondition result;
         result.c_crossattn = std::move(hidden_states);
         result.c_vector    = std::move(pooled);
+        record_conditioning_execution_sequences(execution_evidence, token_and_weights, true);
         return result;
     }

@@ -1365,7 +1469,8 @@ struct FluxCLIPEmbedder : public Conditioner {
         return get_learned_condition_common(n_threads,
                                             tokens_and_weights,
                                             conditioner_params.clip_skip,
-                                            conditioner_params.zero_out_masked);
+                                            conditioner_params.zero_out_masked,
+                                            conditioner_params.execution_evidence);
     }
 };

@@ -1505,7 +1610,8 @@ struct T5CLIPEmbedder : public Conditioner {
     SDCondition get_learned_condition_common(int n_threads,
                                              std::tuple<std::vector<int>, std::vector<float>, std::vector<float>> token_and_weights,
                                              int clip_skip,
-                                             bool zero_out_masked = false) {
+                                             bool zero_out_masked = false,
+                                             ConditioningExecutionEvidence* execution_evidence = nullptr) {
         if (!t5) {
             SDCondition result;
             result.c_crossattn = sd::Tensor<float>::zeros({4096, 256});
@@ -1564,6 +1670,10 @@ struct T5CLIPEmbedder : public Conditioner {
         SDCondition result;
         result.c_crossattn = std::move(hidden_states);
         result.c_vector    = std::move(t5_attn_mask);
+        record_conditioning_execution_sequence(execution_evidence,
+                                               t5_tokens,
+                                               t5_weights,
+                                               true);
         return result;
     }

@@ -1573,7 +1683,8 @@ struct T5CLIPEmbedder : public Conditioner {
         return get_learned_condition_common(n_threads,
                                             tokens_and_weights,
                                             conditioner_params.clip_skip,
-                                            conditioner_params.zero_out_masked);
+                                            conditioner_params.zero_out_masked,
+                                            conditioner_params.execution_evidence);
     }
 };

@@ -1695,6 +1806,10 @@ struct AnimaConditioner : public Conditioner {
         result.c_crossattn  = std::move(hidden_states);
         result.c_t5_ids     = std::move(t5_ids_tensor);
         result.c_t5_weights = std::move(t5_weight_tensor);
+        record_conditioning_execution_sequences(
+            conditioner_params.execution_evidence,
+            {{qwen_tokens, qwen_weights}, {t5_tokens, t5_weights}},
+            true);
         return result;
     }
 };
@@ -1841,7 +1956,8 @@ struct LLMEmbedder : public Conditioner {
                                     const std::set<int>& out_layers,
                                     int prompt_template_encode_start_idx,
                                     bool spell_quotes = false,
-                                    int max_length    = 100000000) {
+                                    int max_length    = 100000000,
+                                    ConditioningExecutionEvidence* execution_evidence = nullptr) {
         auto tokens_weights_mask = tokenize(prompt, prompt_attn_range, min_length, max_length, spell_quotes);
         auto& tokens             = std::get<0>(tokens_weights_mask);
         auto& weights            = std::get<1>(tokens_weights_mask);
@@ -1873,6 +1989,7 @@ struct LLMEmbedder : public Conditioner {
                                           out_layers);
         GGML_ASSERT(!hidden_states.empty());
         hidden_states = apply_token_weights(std::move(hidden_states), weights);
+        record_conditioning_execution_sequence(execution_evidence, tokens, weights, true);
         GGML_ASSERT(hidden_states.shape()[1] > prompt_template_encode_start_idx);

         int64_t zero_pad_len = 0;
@@ -2162,7 +2279,8 @@ struct LLMEmbedder : public Conditioner {
                                                out_layers,
                                                0,
                                                false,
-                                               max_length);
+                                               max_length,
+                                               conditioner_params.execution_evidence);
             GGML_ASSERT(!hidden_states.empty());

             if (hidden_states.shape()[1] > pixeldit_max_length) {
@@ -2193,7 +2311,8 @@ struct LLMEmbedder : public Conditioner {
                                            out_layers,
                                            prompt_template_encode_start_idx,
                                            spell_quotes,
-                                           max_length);
+                                           max_length,
+                                           conditioner_params.execution_evidence);
         std::vector<sd::Tensor<float>> extra_hidden_states_vec;
         for (int i = 0; i < extra_prompts.size(); i++) {
             auto extra_hidden_states = encode_prompt(n_threads,
@@ -2205,7 +2324,8 @@ struct LLMEmbedder : public Conditioner {
                                                      out_layers,
                                                      prompt_template_encode_start_idx,
                                                      spell_quotes,
-                                                     max_length);
+                                                     max_length,
+                                                     conditioner_params.execution_evidence);
             extra_hidden_states_vec.push_back(std::move(extra_hidden_states));
         }

diff --git a/src/stable-diffusion.cpp b/src/stable-diffusion.cpp
index 28598eb..5edca08 100644
--- a/src/stable-diffusion.cpp
+++ b/src/stable-diffusion.cpp
@@ -1,6 +1,9 @@
 #include <algorithm>
 #include <cmath>
+#include <cstdint>
+#include <cstring>
 #include <cstdlib>
+#include <unordered_set>

 #include "ggml_extend.hpp"
 #include "ggml_graph_cut.h"
@@ -136,10 +139,274 @@ static float get_cache_reuse_threshold(const sd_cache_params_t& params) {
     return std::max(0.0f, reuse_threshold);
 }

+enum class DiffusionExecutionBranch : uint32_t {
+    POSITIVE = 1,
+    NEGATIVE = 2,
+    AUXILIARY = 3,
+};
+
+struct ImageExecutionEvidenceState {
+    static constexpr uint32_t VERSION = SD_IMAGE_EXECUTION_EVIDENCE_VERSION;
+
+    sd_image_execution_evidence_t summary = {};
+    std::vector<uint8_t> conditioning_artifact;
+    bool collecting = false;
+
+    void append_u32(uint32_t value) {
+        for (uint32_t shift = 0; shift < 32; shift += 8) {
+            conditioning_artifact.push_back(static_cast<uint8_t>((value >> shift) & 0xffu));
+        }
+    }
+
+    void append_u64(uint64_t value) {
+        for (uint32_t shift = 0; shift < 64; shift += 8) {
+            conditioning_artifact.push_back(static_cast<uint8_t>((value >> shift) & 0xffu));
+        }
+    }
+
+    void reset() {
+        summary = {};
+        summary.version = VERSION;
+        conditioning_artifact.clear();
+        conditioning_artifact.insert(conditioning_artifact.end(), {'I', 'C', 'E', 'V'});
+        append_u32(VERSION);
+        summary.conditioning_artifact_byte_count = conditioning_artifact.size();
+        collecting = true;
+    }
+
+    void stop() {
+        collecting = false;
+    }
+
+    void record_conditioning(DiffusionExecutionBranch branch,
+                             const ConditioningExecutionEvidence& evidence) {
+        if (!collecting || branch == DiffusionExecutionBranch::AUXILIARY) {
+            return;
+        }
+
+        const bool negative = branch == DiffusionExecutionBranch::NEGATIVE;
+        uint32_t* observed = negative
+                                 ? &summary.negative_conditioning_observed
+                                 : &summary.positive_conditioning_observed;
+        uint32_t* weighting_capable = negative
+                                          ? &summary.negative_prompt_weighting_capable
+                                          : &summary.positive_prompt_weighting_capable;
+        uint64_t* token_count = negative
+                                    ? &summary.negative_conditioning_token_count
+                                    : &summary.positive_conditioning_token_count;
+        uint64_t* non_unity_count = negative
+                                        ? &summary.negative_non_unity_weight_count
+                                        : &summary.positive_non_unity_weight_count;
+
+        *observed = evidence.observed() ? 1u : 0u;
+        *weighting_capable = evidence.prompt_weighting_capable() ? 1u : 0u;
+        *token_count = 0;
+        *non_unity_count = 0;
+
+        append_u32(static_cast<uint32_t>(branch));
+        append_u32(*observed);
+        append_u64(static_cast<uint64_t>(evidence.sequences.size()));
+        for (const auto& sequence : evidence.sequences) {
+            append_u32(sequence.weights_applied ? 1u : 0u);
+            append_u32(sequence.counts_toward_token_totals ? 1u : 0u);
+            append_u64(static_cast<uint64_t>(sequence.token_ids.size()));
+            if (sequence.token_ids.size() != sequence.token_weights.size()) {
+                *observed = 0u;
+                continue;
+            }
+            if (sequence.counts_toward_token_totals) {
+                *token_count += static_cast<uint64_t>(sequence.token_ids.size());
+            }
+            for (size_t index = 0; index < sequence.token_ids.size(); ++index) {
+                append_u32(static_cast<uint32_t>(sequence.token_ids[index]));
+                uint32_t weight_bits = 0;
+                static_assert(sizeof(weight_bits) == sizeof(sequence.token_weights[index]),
+                              "conditioning weight must be binary32");
+                std::memcpy(&weight_bits,
+                            &sequence.token_weights[index],
+                            sizeof(weight_bits));
+                append_u32(weight_bits);
+                if (sequence.counts_toward_token_totals &&
+                    sequence.weights_applied &&
+                    sequence.token_weights[index] != 1.0f) {
+                    ++(*non_unity_count);
+                }
+            }
+        }
+        summary.prompt_weighting_applied =
+            summary.positive_non_unity_weight_count > 0 ||
+                    summary.negative_non_unity_weight_count > 0
+                ? 1u
+                : 0u;
+        summary.conditioning_artifact_byte_count = conditioning_artifact.size();
+    }
+
+    void record_sampling_pass() {
+        if (collecting) {
+            ++summary.sampling_pass_count;
+        }
+    }
+
+    void record_completed_sampling_step() {
+        if (collecting) {
+            ++summary.completed_sampling_step_count;
+        }
+    }
+
+    void record_diffusion_compute(DiffusionExecutionBranch branch) {
+        if (!collecting) {
+            return;
+        }
+        ++summary.diffusion_model_compute_count;
+        switch (branch) {
+            case DiffusionExecutionBranch::POSITIVE:
+                ++summary.positive_diffusion_model_compute_count;
+                break;
+            case DiffusionExecutionBranch::NEGATIVE:
+                ++summary.negative_diffusion_model_compute_count;
+                break;
+            case DiffusionExecutionBranch::AUXILIARY:
+                ++summary.auxiliary_diffusion_model_compute_count;
+                break;
+        }
+    }
+
+    void record_control_net_compute_attempt(DiffusionExecutionBranch branch) {
+        if (!collecting) {
+            return;
+        }
+        ++summary.control_net_compute_attempt_count;
+        switch (branch) {
+            case DiffusionExecutionBranch::POSITIVE:
+                ++summary.positive_control_net_compute_attempt_count;
+                break;
+            case DiffusionExecutionBranch::NEGATIVE:
+                ++summary.negative_control_net_compute_attempt_count;
+                break;
+            case DiffusionExecutionBranch::AUXILIARY:
+                break;
+        }
+    }
+
+    void record_control_net_compute_success(DiffusionExecutionBranch branch) {
+        if (!collecting) {
+            return;
+        }
+        ++summary.control_net_compute_success_count;
+        switch (branch) {
+            case DiffusionExecutionBranch::POSITIVE:
+                ++summary.positive_control_net_compute_success_count;
+                break;
+            case DiffusionExecutionBranch::NEGATIVE:
+                ++summary.negative_control_net_compute_success_count;
+                break;
+            case DiffusionExecutionBranch::AUXILIARY:
+                break;
+        }
+    }
+
+    void record_control_net_residual_consumption(DiffusionExecutionBranch branch) {
+        if (!collecting) {
+            return;
+        }
+        ++summary.control_net_residual_consumption_count;
+        switch (branch) {
+            case DiffusionExecutionBranch::POSITIVE:
+                ++summary.positive_control_net_residual_consumption_count;
+                break;
+            case DiffusionExecutionBranch::NEGATIVE:
+                ++summary.negative_control_net_residual_consumption_count;
+                break;
+            case DiffusionExecutionBranch::AUXILIARY:
+                ++summary.auxiliary_control_net_residual_consumption_count;
+                break;
+        }
+    }
+
+    void record_lora_requested(uint32_t count) {
+        if (collecting) {
+            summary.lora_requested_count = count;
+            summary.lora_loaded_count = 0u;
+            summary.lora_applied_count = 0u;
+            summary.lora_applied_tensor_count = 0u;
+        }
+    }
+
+    void record_lora_loaded(uint32_t count) {
+        if (collecting) {
+            summary.lora_loaded_count = count;
+        }
+    }
+
+    void record_lora_applied(uint32_t count, uint64_t tensor_count) {
+        if (collecting) {
+            summary.lora_applied_count = count;
+            summary.lora_applied_tensor_count = tensor_count;
+        }
+    }
+
+    void mark_generation_completed(bool flow_shift_applied,
+                                   bool dynamic_flow_shift,
+                                   float effective_flow_shift,
+                                   bool distilled_guidance_applied,
+                                   float effective_distilled_guidance) {
+        if (collecting) {
+            summary.generation_completed = 1u;
+            summary.flow_shift_applied = flow_shift_applied ? 1u : 0u;
+            summary.dynamic_flow_shift = dynamic_flow_shift ? 1u : 0u;
+            summary.distilled_guidance_applied = distilled_guidance_applied ? 1u : 0u;
+            summary.effective_flow_shift = flow_shift_applied ? effective_flow_shift : 0.0f;
+            summary.effective_distilled_guidance = distilled_guidance_applied
+                                                       ? effective_distilled_guidance
+                                                       : 0.0f;
+        }
+    }
+};
+
+struct ImageExecutionEvidenceScope {
+    explicit ImageExecutionEvidenceScope(ImageExecutionEvidenceState& evidence)
+        : evidence(evidence) {
+        evidence.reset();
+    }
+
+    ~ImageExecutionEvidenceScope() {
+        evidence.stop();
+    }
+
+    ImageExecutionEvidenceState& evidence;
+};
+
+struct VaeImageExecutionEvidenceScope {
+    VaeImageExecutionEvidenceScope(const std::shared_ptr<VAE>& first_stage_model,
+                                   const std::shared_ptr<VAE>& preview_vae,
+                                   sd_image_execution_evidence_t* evidence)
+        : first_stage_model(first_stage_model), preview_vae(preview_vae) {
+        if (this->first_stage_model != nullptr) {
+            this->first_stage_model->set_image_execution_evidence(evidence);
+        }
+        if (this->preview_vae != nullptr) {
+            this->preview_vae->set_image_execution_evidence(evidence);
+        }
+    }
+
+    ~VaeImageExecutionEvidenceScope() {
+        if (first_stage_model != nullptr) {
+            first_stage_model->set_image_execution_evidence(nullptr);
+        }
+        if (preview_vae != nullptr) {
+            preview_vae->set_image_execution_evidence(nullptr);
+        }
+    }
+
+    std::shared_ptr<VAE> first_stage_model;
+    std::shared_ptr<VAE> preview_vae;
+};
+
 /*=============================================== StableDiffusionGGML ================================================*/

 class StableDiffusionGGML {
 public:
+    ImageExecutionEvidenceState image_execution_evidence;
     std::vector<MmapTensorStore> mmap_tensor_store;
     SDBackendManager backend_manager;

@@ -155,6 +422,7 @@ public:
     std::shared_ptr<RNG> sampler_rng = nullptr;
     int n_threads                    = -1;
     float default_flow_shift         = INFINITY;
+    bool distilled_guidance_input_enabled = false;

     std::shared_ptr<Conditioner> cond_stage_model;
     std::shared_ptr<FrozenCLIPVisionEmbedder> clip_vision;  // for svd or wan2.1 i2v
@@ -170,6 +438,7 @@ public:
     std::vector<std::shared_ptr<LoraModel>> cond_stage_lora_models;
     std::vector<std::shared_ptr<LoraModel>> diffusion_lora_models;
     std::vector<std::shared_ptr<LoraModel>> first_stage_lora_models;
+    std::unordered_set<std::string> requested_lora_ids;
     bool apply_lora_immediately = false;

     std::string taesd_path;
@@ -1141,6 +1410,19 @@ public:
                 params_memory_location(pmid_params_mem_size, SDBackendModule::PHOTOMAKER));
         }

+        distilled_guidance_input_enabled = false;
+        if (sd_version_is_flux(version) ||
+            sd_version_is_flux2(version) ||
+            sd_version_is_longcat(version)) {
+            for (const auto& [name, tensor_storage] : tensor_storage_map) {
+                (void)tensor_storage;
+                if (name.find("guidance_in.in_layer.weight") != std::string::npos) {
+                    distilled_guidance_input_enabled = true;
+                    break;
+                }
+            }
+        }
+
         // init denoiser
         {
             prediction_t pred_type = sd_ctx_params->prediction;
@@ -1493,13 +1775,33 @@ public:
                 first_stage_model->set_weight_adapter(multi_lora_adapter);
             }
         }
+        std::unordered_set<std::string> loaded_ids;
+        const auto collect_loaded = [&](const std::vector<std::shared_ptr<LoraModel>>& models) {
+            for (const auto& model : models) {
+                if (model && !model->lora_tensors.empty()) loaded_ids.insert(model->lora_id);
+            }
+        };
+        collect_loaded(cond_stage_lora_models);
+        collect_loaded(diffusion_lora_models);
+        collect_loaded(first_stage_lora_models);
+        image_execution_evidence.record_lora_loaded(static_cast<uint32_t>(loaded_ids.size()));
     }

     void lora_stat() {
+        std::unordered_set<std::string> applied_ids;
+        uint64_t applied_tensor_count = 0u;
+        const auto collect_applied = [&](const std::shared_ptr<LoraModel>& lora_model) {
+            if (lora_model && !lora_model->applied_lora_tensors.empty()) {
+                applied_ids.insert(lora_model->lora_id);
+                applied_tensor_count +=
+                    static_cast<uint64_t>(lora_model->applied_lora_tensors.size());
+            }
+        };
         if (!cond_stage_lora_models.empty()) {
             LOG_INFO("cond_stage_lora_models:");
             for (auto& lora_model : cond_stage_lora_models) {
                 lora_model->stat();
+                collect_applied(lora_model);
             }
         }

@@ -1507,6 +1809,7 @@ public:
             LOG_INFO("diffusion_lora_models:");
             for (auto& lora_model : diffusion_lora_models) {
                 lora_model->stat();
+                collect_applied(lora_model);
             }
         }

@@ -1514,20 +1817,28 @@ public:
             LOG_INFO("first_stage_lora_models:");
             for (auto& lora_model : first_stage_lora_models) {
                 lora_model->stat();
+                collect_applied(lora_model);
             }
         }
+        image_execution_evidence.record_lora_applied(
+            static_cast<uint32_t>(applied_ids.size()),
+            applied_tensor_count);
     }

     void apply_loras(const sd_lora_t* loras, uint32_t lora_count) {
         std::unordered_map<std::string, float> lora_f2m;
+        requested_lora_ids.clear();
         for (uint32_t i = 0; i < lora_count; i++) {
             std::string lora_id = SAFE_STR(loras[i].path);
             if (loras[i].is_high_noise) {
                 lora_id = "|high_noise|" + lora_id;
             }
             lora_f2m[lora_id] = loras[i].multiplier;
+            requested_lora_ids.insert(lora_id);
             LOG_DEBUG("lora %s:%.2f", lora_id.c_str(), loras[i].multiplier);
         }
+        image_execution_evidence.record_lora_requested(
+            static_cast<uint32_t>(requested_lora_ids.size()));
         int64_t t0 = ggml_time_ms();
         if (apply_lora_immediately) {
             apply_loras_immediately(lora_f2m);
@@ -1786,14 +2097,29 @@ public:
         if (preview_mode == PREVIEW_VAE || preview_mode == PREVIEW_TAE) {
             sd::Tensor<float> vae_latents;
             sd::Tensor<float> decoded;
+            const bool record_preview_vae_execution = false;
             if (preview_vae) {
                 preview_vae->set_temporal_tiling_enabled(vae_tiling_params.temporal_tiling);
                 vae_latents = preview_vae->diffusion_to_vae_latents(_latents);
-                decoded     = preview_vae->decode(n_threads, vae_latents, vae_tiling_params, is_video, circular_x, circular_y, true);
+                decoded     = preview_vae->decode(n_threads,
+                                                  vae_latents,
+                                                  vae_tiling_params,
+                                                  is_video,
+                                                  circular_x,
+                                                  circular_y,
+                                                  true,
+                                                  record_preview_vae_execution);
             } else {
                 first_stage_model->set_temporal_tiling_enabled(vae_tiling_params.temporal_tiling);
                 vae_latents = first_stage_model->diffusion_to_vae_latents(_latents);
-                decoded     = first_stage_model->decode(n_threads, vae_latents, vae_tiling_params, is_video, circular_x, circular_y, true);
+                decoded     = first_stage_model->decode(n_threads,
+                                                        vae_latents,
+                                                        vae_tiling_params,
+                                                        is_video,
+                                                        circular_x,
+                                                        circular_y,
+                                                        true,
+                                                        record_preview_vae_execution);
             }
             if (decoded.empty()) {
                 LOG_ERROR("preview decode failed at step %d", step);
@@ -1878,6 +2204,7 @@ public:
     }

     void report_sample_progress(int step, size_t total_steps, int64_t t0) {
+        image_execution_evidence.record_completed_sampling_step();
         int64_t t1 = ggml_time_us();
         if (step > 0 || step == -(int)total_steps) {
             int showstep = std::abs(step);
@@ -1885,15 +2212,22 @@ public:
         }
     }

-    void compute_sample_controls(const sd::Tensor<float>& control_image,
+    bool compute_sample_controls(const sd::Tensor<float>& control_image,
                                  const sd::Tensor<float>& noised_input,
                                  const sd::Tensor<float>& timesteps_tensor,
                                  const SDCondition& condition,
+                                 DiffusionExecutionBranch execution_branch,
                                  std::vector<sd::Tensor<float>>* controls) {
         GGML_ASSERT(controls != nullptr);
         controls->clear();
-        if (control_image.empty() || control_net == nullptr) {
-            return;
+        if (control_image.empty()) {
+            return true;
+        }
+
+        image_execution_evidence.record_control_net_compute_attempt(execution_branch);
+        if (control_net == nullptr) {
+            LOG_ERROR("control image was provided without a loaded ControlNet");
+            return false;
         }

         auto control_result = control_net->compute(n_threads,
@@ -1902,12 +2236,14 @@ public:
                                                    timesteps_tensor,
                                                    condition.c_crossattn,
                                                    condition.c_vector);
-        if (!control_result.has_value()) {
+        if (!control_result.has_value() || control_result->empty()) {
             LOG_ERROR("controlnet compute failed");
-            return;
+            return false;
         }

         *controls = std::move(*control_result);
+        image_execution_evidence.record_control_net_compute_success(execution_branch);
+        return true;
     }

     sd::Tensor<float> sample(const std::shared_ptr<DiffusionModelRunner>& work_diffusion_model,
@@ -1958,6 +2294,7 @@ public:
         }

         size_t steps       = sigmas.size() - 1;
+        image_execution_evidence.record_sampling_pass();
         bool has_skiplayer = (slg_scale != 0.0f || slg_uncond) && !skip_layers.empty();
         if (has_skiplayer && !sd_version_is_dit(version)) {
             has_skiplayer = false;
@@ -1990,6 +2327,10 @@ public:
         SamplePreviewContext preview = prepare_sample_preview_context();

         auto denoise = [&](const sd::Tensor<float>& x, float sigma, int step) -> sd::guidance::GuiderOutput {
+            if (sd_should_cancel()) {
+                LOG_WARN("sampling cancelled before step %d", step);
+                return {};
+            }
             if (step == 1 || step == -1) {
                 pretty_progress(0, (int)steps, 0);
             }
@@ -2053,13 +2394,17 @@ public:
             step_guidance_input.schedule_size = sigmas.size();
             bool is_skiplayer_step            = skip_layer_guidance.is_enabled_for_step(step_guidance_input);

-            compute_sample_controls(control_image,
-                                    noised_input,
-                                    timesteps_tensor,
-                                    cond,
-                                    &controls);
+            if (!compute_sample_controls(control_image,
+                                         noised_input,
+                                         timesteps_tensor,
+                                         cond,
+                                         DiffusionExecutionBranch::POSITIVE,
+                                         &controls)) {
+                return {};
+            }

             auto run_condition = [&](const SDCondition& condition,
+                                     DiffusionExecutionBranch execution_branch,
                                      const sd::Tensor<float>* c_concat_override = nullptr,
                                      const std::vector<int>* local_skip_layers  = nullptr) -> sd::Tensor<float> {
                 diffusion_params.context     = condition.c_crossattn.empty() ? nullptr : &condition.c_crossattn;
@@ -2103,7 +2448,24 @@ public:
                     return std::move(cached_output);
                 }

+                if (sd_should_cancel()) {
+                    LOG_WARN("sampling cancelled before diffusion compute at step %d", step);
+                    return sd::Tensor<float>();
+                }
+                const bool control_residuals_wired =
+                    sd_version_is_unet(version) && !controls.empty();
                 auto output_opt = work_diffusion_model->compute(n_threads, diffusion_params);
+                if (!output_opt.empty()) {
+                    image_execution_evidence.record_diffusion_compute(execution_branch);
+                    if (control_residuals_wired) {
+                        image_execution_evidence.record_control_net_residual_consumption(
+                            execution_branch);
+                    }
+                }
+                if (sd_should_cancel()) {
+                    LOG_WARN("sampling cancelled after diffusion compute at step %d", step);
+                    return sd::Tensor<float>();
+                }
                 if (output_opt.empty()) {
                     LOG_ERROR("diffusion model compute failed");
                     return sd::Tensor<float>();
@@ -2114,13 +2476,14 @@ public:
             };

             if (start_merge_step == -1 || step <= start_merge_step) {
-                cond_out = run_condition(cond);
+                cond_out = run_condition(cond, DiffusionExecutionBranch::POSITIVE);
                 if (cond_out.empty()) {
                     return {};
                 }
             } else {
                 GGML_ASSERT(!id_cond.empty());
                 cond_out = run_condition(id_cond,
+                                         DiffusionExecutionBranch::POSITIVE,
                                          cond.c_concat.empty() ? nullptr : &cond.c_concat);
                 if (cond_out.empty()) {
                     return {};
@@ -2129,24 +2492,31 @@ public:

             if (!uncond.empty()) {
                 if (!step_cache.is_step_skipped()) {
-                    compute_sample_controls(control_image,
-                                            noised_input,
-                                            timesteps_tensor,
-                                            uncond,
-                                            &controls);
+                    if (!compute_sample_controls(control_image,
+                                                 noised_input,
+                                                 timesteps_tensor,
+                                                 uncond,
+                                                 DiffusionExecutionBranch::NEGATIVE,
+                                                 &controls)) {
+                        return {};
+                    }
                 }
                 const std::vector<int>* uncond_skip_layers = nullptr;
                 if (is_skiplayer_step && slg_uncond) {
                     LOG_DEBUG("Skipping layers at uncond step %d\n", step);
                     uncond_skip_layers = &skip_layer_guidance.layers();
                 }
-                uncond_out = run_condition(uncond, nullptr, uncond_skip_layers);
+                uncond_out = run_condition(uncond,
+                                           DiffusionExecutionBranch::NEGATIVE,
+                                           nullptr,
+                                           uncond_skip_layers);
                 if (uncond_out.empty()) {
                     return {};
                 }
             }
             if (!img_cond.empty()) {
                 img_cond_out = run_condition(img_cond,
+                                             DiffusionExecutionBranch::AUXILIARY,
                                              cond.c_concat.empty() ? nullptr : &cond.c_concat);
                 if (img_cond_out.empty()) {
                     return {};
@@ -2169,6 +2539,7 @@ public:
                 if (!step_cache.is_step_skipped()) {
                     guidance_input.predict_skip_layer = [&]() -> sd::Tensor<float> {
                         return run_condition(cond,
+                                             DiffusionExecutionBranch::AUXILIARY,
                                              cond.c_concat.empty() ? nullptr : &cond.c_concat,
                                              &skip_layer_guidance.layers());
                     };
@@ -2197,13 +2568,21 @@ public:
                 preview_image(step, denoised, version, preview.mode, preview.callback, preview.data, false);
             }
             report_sample_progress(step, steps, t0);
+            if (sd_should_cancel()) {
+                LOG_WARN("sampling cancelled after step %d", step);
+                return {};
+            }
             output.pred = denoised;
             return output;
         };

         auto x0_opt = sample_k_diffusion(method, denoise, x_t, sigmas, sampler_rng, eta, is_flow_denoiser, extra_sample_args);
         if (x0_opt.empty()) {
-            LOG_ERROR("Diffusion model sampling failed");
+            if (sd_should_cancel()) {
+                LOG_WARN("Diffusion model sampling cancelled");
+            } else {
+                LOG_ERROR("Diffusion model sampling failed");
+            }
             if (control_net) {
                 control_net->free_control_ctx();
                 control_net->free_compute_buffer();
@@ -2386,6 +2765,26 @@ public:
         }
     }

+    bool get_effective_flow_shift(float* flow_shift) const {
+        if (flow_shift == nullptr) {
+            return false;
+        }
+        auto flow_denoiser = std::dynamic_pointer_cast<DiscreteFlowDenoiser>(denoiser);
+        if (!flow_denoiser || !std::isfinite(flow_denoiser->shift)) {
+            return false;
+        }
+        *flow_shift = flow_denoiser->shift;
+        return true;
+    }
+
+    bool uses_dynamic_flow_shift() const {
+        return std::dynamic_pointer_cast<Flux2FlowDenoiser>(denoiser) != nullptr;
+    }
+
+    bool uses_distilled_guidance() const {
+        return distilled_guidance_input_enabled;
+    }
+
     bool is_flow_denoiser() {
         auto flow_denoiser = std::dynamic_pointer_cast<DiscreteFlowDenoiser>(denoiser);
         return !!flow_denoiser;
@@ -3058,6 +3457,54 @@ SD_API bool sd_ctx_supports_video_generation(const sd_ctx_t* sd_ctx) {
     return sd_version_supports_video_generation(sd_ctx->sd->version);
 }

+SD_API bool sd_ctx_uses_flow_shift(const sd_ctx_t* sd_ctx) {
+    if (sd_ctx == nullptr || sd_ctx->sd == nullptr) {
+        return false;
+    }
+    float flow_shift = 0.0f;
+    return sd_ctx->sd->get_effective_flow_shift(&flow_shift);
+}
+
+SD_API bool sd_ctx_uses_dynamic_flow_shift(const sd_ctx_t* sd_ctx) {
+    return sd_ctx != nullptr && sd_ctx->sd != nullptr &&
+           sd_ctx->sd->uses_dynamic_flow_shift();
+}
+
+SD_API bool sd_ctx_uses_distilled_guidance(const sd_ctx_t* sd_ctx) {
+    return sd_ctx != nullptr && sd_ctx->sd != nullptr &&
+           sd_ctx->sd->uses_distilled_guidance();
+}
+
+SD_API bool sd_get_last_image_execution_evidence(const sd_ctx_t* sd_ctx,
+                                                  sd_image_execution_evidence_t* evidence) {
+    if (sd_ctx == nullptr || sd_ctx->sd == nullptr || evidence == nullptr) {
+        return false;
+    }
+    const ImageExecutionEvidenceState& state = sd_ctx->sd->image_execution_evidence;
+    if (state.collecting || state.summary.version != ImageExecutionEvidenceState::VERSION) {
+        return false;
+    }
+    *evidence = state.summary;
+    return true;
+}
+
+SD_API bool sd_copy_last_image_conditioning_artifact(const sd_ctx_t* sd_ctx,
+                                                      uint8_t* destination,
+                                                      size_t destination_size) {
+    if (sd_ctx == nullptr || sd_ctx->sd == nullptr || destination == nullptr) {
+        return false;
+    }
+    const ImageExecutionEvidenceState& state = sd_ctx->sd->image_execution_evidence;
+    if (state.collecting || state.summary.version != ImageExecutionEvidenceState::VERSION ||
+        destination_size != state.conditioning_artifact.size()) {
+        return false;
+    }
+    std::copy(state.conditioning_artifact.begin(),
+              state.conditioning_artifact.end(),
+              destination);
+    return true;
+}
+
 enum sample_method_t sd_get_default_sample_method(const sd_ctx_t* sd_ctx) {
     if (sd_ctx != nullptr && sd_ctx->sd != nullptr) {
         if (sd_version_is_pid(sd_ctx->sd->version)) {
@@ -4098,13 +4545,19 @@ static std::optional<ImageGenerationEmbeds> prepare_image_generation_embeds(sd_c
     auto id_cond                     = sd_ctx->sd->get_pmid_conditon(request->pm_params, condition_params);
     int64_t prepare_start_ms         = ggml_time_ms();
     condition_params.zero_out_masked = false;
+    ConditioningExecutionEvidence positive_execution_evidence;
+    condition_params.execution_evidence = &positive_execution_evidence;
     auto cond                        = sd_ctx->sd->cond_stage_model->get_learned_condition(sd_ctx->sd->n_threads,
                                                                                            condition_params);
+    sd_ctx->sd->image_execution_evidence.record_conditioning(
+        DiffusionExecutionBranch::POSITIVE,
+        positive_execution_evidence);
     if (cond.c_concat.empty()) {
         cond.c_concat = latents->concat_latent;  // TODO: optimize
     }

     SDCondition uncond;
+    ConditioningExecutionEvidence negative_execution_evidence;
     if (request->use_uncond || request->use_high_noise_uncond) {
         bool zero_out_masked = false;
         if (sd_version_is_sdxl(sd_ctx->sd->version) &&
@@ -4114,12 +4567,17 @@ static std::optional<ImageGenerationEmbeds> prepare_image_generation_embeds(sd_c
         }
         condition_params.text            = request->negative_prompt;
         condition_params.zero_out_masked = zero_out_masked;
+        condition_params.execution_evidence = &negative_execution_evidence;
         uncond                           = sd_ctx->sd->cond_stage_model->get_learned_condition(sd_ctx->sd->n_threads,
                                                                                                condition_params);
         if (uncond.c_concat.empty()) {
             uncond.c_concat = latents->uncond_concat_latent;  // TODO: optimize
         }
     }
+    sd_ctx->sd->image_execution_evidence.record_conditioning(
+        DiffusionExecutionBranch::NEGATIVE,
+        negative_execution_evidence);
+    condition_params.execution_evidence = nullptr;

     int64_t t1 = ggml_time_ms();
     LOG_INFO("get_learned_condition completed, taking %.2fs", (t1 - prepare_start_ms) * 1.0f / 1000);
@@ -4364,6 +4822,11 @@ SD_API sd_image_t* generate_image(sd_ctx_t* sd_ctx, const sd_img_gen_params_t* s
     if (sd_ctx == nullptr || sd_img_gen_params == nullptr) {
         return nullptr;
     }
+    ImageExecutionEvidenceScope image_execution_scope(sd_ctx->sd->image_execution_evidence);
+    VaeImageExecutionEvidenceScope vae_execution_scope(
+        sd_ctx->sd->first_stage_model,
+        sd_ctx->sd->preview_vae,
+        &sd_ctx->sd->image_execution_evidence.summary);

     int64_t t0                    = ggml_time_ms();
     sd_ctx->sd->vae_tiling_params = sd_img_gen_params->vae_tiling_params;
@@ -4382,6 +4845,10 @@ SD_API sd_image_t* generate_image(sd_ctx_t* sd_ctx, const sd_img_gen_params_t* s
                                                         sd_img_gen_params,
                                                         &request,
                                                         &plan);
+    if (sd_should_cancel()) {
+        LOG_WARN("image generation cancelled after latent preparation");
+        return nullptr;
+    }
     if (!latents_opt.has_value()) {
         return nullptr;
     }
@@ -4392,6 +4859,10 @@ SD_API sd_image_t* generate_image(sd_ctx_t* sd_ctx, const sd_img_gen_params_t* s
                                                       &request,
                                                       &plan,
                                                       &latents);
+    if (sd_should_cancel()) {
+        LOG_WARN("image generation cancelled after embedding preparation");
+        return nullptr;
+    }
     if (!embeds_opt.has_value()) {
         return nullptr;
     }
@@ -4403,6 +4874,10 @@ SD_API sd_image_t* generate_image(sd_ctx_t* sd_ctx, const sd_img_gen_params_t* s
         int64_t sampling_start = ggml_time_ms();
         int64_t cur_seed       = request.seed + b;
         LOG_INFO("generating image: %i/%i - seed %" PRId64, b + 1, request.batch_count, cur_seed);
+        if (sd_should_cancel()) {
+            LOG_WARN("image generation cancelled before sampling batch %d", b + 1);
+            return nullptr;
+        }

         sd_ctx->sd->rng->manual_seed(cur_seed);
         sd_ctx->sd->sampler_rng->manual_seed(cur_seed);
@@ -4583,6 +5058,16 @@ SD_API sd_image_t* generate_image(sd_ctx_t* sd_ctx, const sd_img_gen_params_t* s
     }

     auto result = decode_image_outputs(sd_ctx, request, final_latents);
+    if (sd_should_cancel()) {
+        LOG_WARN("image generation cancelled after decode");
+        if (result != nullptr) {
+            for (int i = 0; i < request.batch_count; i++) {
+                free(result[i].data);
+            }
+            free(result);
+        }
+        return nullptr;
+    }
     if (result == nullptr) {
         return nullptr;
     }
@@ -4591,6 +5076,15 @@ SD_API sd_image_t* generate_image(sd_ctx_t* sd_ctx, const sd_img_gen_params_t* s

     int64_t t1 = ggml_time_ms();
     LOG_INFO("generate_image completed in %.2fs", (t1 - t0) * 1.0f / 1000);
+    float effective_flow_shift = 0.0f;
+    const bool flow_shift_applied =
+        sd_ctx->sd->get_effective_flow_shift(&effective_flow_shift);
+    sd_ctx->sd->image_execution_evidence.mark_generation_completed(
+        flow_shift_applied,
+        sd_ctx->sd->uses_dynamic_flow_shift(),
+        effective_flow_shift,
+        sd_ctx->sd->uses_distilled_guidance(),
+        request.guidance.distilled_guidance);
     return result;
 }

diff --git a/src/tokenizers/vocab/vocab.cpp b/src/tokenizers/vocab/vocab.cpp
index 4d2a9b8..7763fb6 100644
--- a/src/tokenizers/vocab/vocab.cpp
+++ b/src/tokenizers/vocab/vocab.cpp
@@ -1,5 +1,9 @@
 #include "vocab.h"
 #include "clip_merges.hpp"
+#include "qwen_merges.hpp"
+#include "t5.hpp"
+
+#if !defined(__ANDROID__)
 #include "gemma2_merges.hpp"
 #include "gemma2_vocab.hpp"
 #include "gemma_merges.hpp"
@@ -8,9 +12,8 @@
 #include "gpt_oss_vocab.hpp"
 #include "mistral_merges.hpp"
 #include "mistral_vocab.hpp"
-#include "qwen_merges.hpp"
-#include "t5.hpp"
 #include "umt5.hpp"
+#endif

 std::string load_clip_merges() {
     std::string merges_utf8_str(reinterpret_cast<const char*>(clip_merges_utf8_c_str), sizeof(clip_merges_utf8_c_str));
@@ -23,13 +26,21 @@ std::string load_qwen2_merges() {
 }

 std::string load_mistral_merges() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string merges_utf8_str(reinterpret_cast<const char*>(mistral_merges_utf8_c_str), sizeof(mistral_merges_utf8_c_str));
     return merges_utf8_str;
+#endif
 }

 std::string load_mistral_vocab_json() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string json_str(reinterpret_cast<const char*>(mistral_vocab_json_utf8_c_str), sizeof(mistral_vocab_json_utf8_c_str));
     return json_str;
+#endif
 }

 std::string load_t5_tokenizer_json() {
@@ -38,36 +49,64 @@ std::string load_t5_tokenizer_json() {
 }

 std::string load_umt5_tokenizer_json() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string json_str(reinterpret_cast<const char*>(umt5_tokenizer_json_str), sizeof(umt5_tokenizer_json_str));
     return json_str;
+#endif
 }

 std::string load_gemma_merges() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string merges_utf8_str(reinterpret_cast<const char*>(gemma_merges_utf8_c_str), sizeof(gemma_merges_utf8_c_str));
     return merges_utf8_str;
+#endif
 }

 std::string load_gemma_vocab_json() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string json_str(reinterpret_cast<const char*>(gemma_vocab_json_utf8_c_str), sizeof(gemma_vocab_json_utf8_c_str));
     return json_str;
+#endif
 }

 std::string load_gemma2_merges() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string merges_utf8_str(reinterpret_cast<const char*>(gemma2_merges_utf8_c_str), sizeof(gemma2_merges_utf8_c_str));
     return merges_utf8_str;
+#endif
 }

 std::string load_gemma2_vocab_json() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string json_str(reinterpret_cast<const char*>(gemma2_vocab_json_utf8_c_str), sizeof(gemma2_vocab_json_utf8_c_str));
     return json_str;
+#endif
 }

 std::string load_gpt_oss_merges() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string merges_utf8_str(reinterpret_cast<const char*>(gpt_oss_merges_utf8_c_str), sizeof(gpt_oss_merges_utf8_c_str));
     return merges_utf8_str;
+#endif
 }

 std::string load_gpt_oss_vocab_json() {
+#if defined(__ANDROID__)
+    return {};
+#else
     std::string json_str(reinterpret_cast<const char*>(gpt_oss_vocab_json_utf8_c_str), sizeof(gpt_oss_vocab_json_utf8_c_str));
     return json_str;
-}
\ No newline at end of file
+#endif
+}
diff --git a/src/upscaler.cpp b/src/upscaler.cpp
index 1197ce3..730a56e 100644
--- a/src/upscaler.cpp
+++ b/src/upscaler.cpp
@@ -4,8 +4,52 @@
 #include "stable-diffusion.h"
 #include "util.h"

+#include <cstdint>
+#include <memory>
+#include <new>
 #include <utility>

+namespace {
+
+thread_local sd_upscaler_execution_evidence_t* active_upscaler_evidence = nullptr;
+
+class UpscalerEvidenceScope {
+public:
+    explicit UpscalerEvidenceScope(sd_upscaler_execution_evidence_t* evidence)
+        : previous(active_upscaler_evidence) {
+        active_upscaler_evidence = evidence;
+    }
+
+    ~UpscalerEvidenceScope() {
+        active_upscaler_evidence = previous;
+    }
+
+private:
+    sd_upscaler_execution_evidence_t* previous;
+};
+
+void record_compute_start(bool tile) {
+    if (active_upscaler_evidence == nullptr) {
+        return;
+    }
+    ++active_upscaler_evidence->compute_invocation_count;
+    if (tile) {
+        ++active_upscaler_evidence->tile_compute_invocation_count;
+    }
+}
+
+void record_compute_result(bool tile, bool succeeded) {
+    if (active_upscaler_evidence == nullptr || !succeeded) {
+        return;
+    }
+    ++active_upscaler_evidence->compute_success_count;
+    if (tile) {
+        ++active_upscaler_evidence->tile_compute_success_count;
+    }
+}
+
+}  // namespace
+
 UpscalerGGML::UpscalerGGML(int n_threads,
                            bool direct,
                            int tile_size,
@@ -68,6 +112,7 @@ bool UpscalerGGML::load_from_file(const std::string& esrgan_path,
     ModelLoader model_loader;
     if (!model_loader.init_from_file_and_convert_name(esrgan_path)) {
         LOG_ERROR("init model loader from file failed: '%s'", esrgan_path.c_str());
+        return false;
     }
     model_loader.set_wtype_override(model_data_type);
     LOG_INFO("Upscaler weight type: %s", ggml_type_name(model_data_type));
@@ -87,12 +132,23 @@ bool UpscalerGGML::load_from_file(const std::string& esrgan_path,

 sd::Tensor<float> UpscalerGGML::upscale_tensor(const sd::Tensor<float>& input_tensor) {
     sd::Tensor<float> upscaled;
+    if (sd_should_cancel()) {
+        LOG_WARN("esrgan upscale cancelled before execution");
+        return {};
+    }
     if (tile_size <= 0 || (input_tensor.shape()[0] <= tile_size && input_tensor.shape()[1] <= tile_size)) {
+        record_compute_start(false);
         upscaled = esrgan_upscaler->compute(n_threads, input_tensor);
+        record_compute_result(false, !upscaled.empty());
     } else {
         auto on_processing = [&](const sd::Tensor<float>& input_tile) -> sd::Tensor<float> {
+            if (sd_should_cancel()) {
+                return {};
+            }
+            record_compute_start(true);
             auto output_tile = esrgan_upscaler->compute(n_threads, input_tile);
-            if (output_tile.empty()) {
+            record_compute_result(true, !output_tile.empty());
+            if (output_tile.empty() || sd_should_cancel()) {
                 LOG_ERROR("esrgan compute failed while processing a tile");
                 return {};
             }
@@ -111,7 +167,10 @@ sd::Tensor<float> UpscalerGGML::upscale_tensor(const sd::Tensor<float>& input_te
                                     on_processing);
     }
     esrgan_upscaler->free_compute_buffer();
-    if (upscaled.empty()) {
+    if (upscaled.empty() || sd_should_cancel()) {
+        if (sd_should_cancel()) {
+            LOG_WARN("esrgan upscale cancelled");
+        }
         LOG_ERROR("esrgan compute failed");
         return {};
     }
@@ -119,7 +178,7 @@ sd::Tensor<float> UpscalerGGML::upscale_tensor(const sd::Tensor<float>& input_te
 }

 sd_image_t UpscalerGGML::upscale(sd_image_t input_image, uint32_t upscale_factor) {
-    // upscale_factor, unused for RealESRGAN_x4plus_anime_6B.pth
+    // The public wrapper validates upscale_factor against the loaded model scale.
     sd_image_t upscaled_image = {0, 0, 0, nullptr};
     int output_width          = (int)input_image.width * esrgan_upscaler->scale;
     int output_height         = (int)input_image.height * esrgan_upscaler->scale;
@@ -142,6 +201,7 @@ sd_image_t UpscalerGGML::upscale(sd_image_t input_image, uint32_t upscale_factor

 struct upscaler_ctx_t {
     UpscalerGGML* upscaler = nullptr;
+    sd_upscaler_execution_evidence_t execution_evidence{};
 };

 upscaler_ctx_t* new_upscaler_ctx(const char* esrgan_path_c_str,
@@ -151,28 +211,72 @@ upscaler_ctx_t* new_upscaler_ctx(const char* esrgan_path_c_str,
                                  int tile_size,
                                  const char* backend,
                                  const char* params_backend) {
-    upscaler_ctx_t* upscaler_ctx = (upscaler_ctx_t*)malloc(sizeof(upscaler_ctx_t));
-    if (upscaler_ctx == nullptr) {
+    std::unique_ptr<upscaler_ctx_t> upscaler_ctx(new (std::nothrow) upscaler_ctx_t());
+    if (!upscaler_ctx) {
         return nullptr;
     }
     std::string esrgan_path(esrgan_path_c_str);

-    upscaler_ctx->upscaler = new UpscalerGGML(n_threads, direct, tile_size, SAFE_STR(backend), SAFE_STR(params_backend));
-    if (upscaler_ctx->upscaler == nullptr) {
+    std::unique_ptr<UpscalerGGML> upscaler(new (std::nothrow) UpscalerGGML(
+        n_threads, direct, tile_size, SAFE_STR(backend), SAFE_STR(params_backend)));
+    if (!upscaler) {
         return nullptr;
     }

-    if (!upscaler_ctx->upscaler->load_from_file(esrgan_path, offload_params_to_cpu, n_threads)) {
-        delete upscaler_ctx->upscaler;
-        upscaler_ctx->upscaler = nullptr;
-        free(upscaler_ctx);
+    if (!upscaler->load_from_file(esrgan_path, offload_params_to_cpu, n_threads)) {
         return nullptr;
     }
-    return upscaler_ctx;
+    upscaler_ctx->upscaler = upscaler.release();
+    return upscaler_ctx.release();
 }

 sd_image_t upscale(upscaler_ctx_t* upscaler_ctx, sd_image_t input_image, uint32_t upscale_factor) {
-    return upscaler_ctx->upscaler->upscale(input_image, upscale_factor);
+    sd_image_t empty = {0, 0, 0, nullptr};
+    if (upscaler_ctx == nullptr || upscaler_ctx->upscaler == nullptr ||
+        upscaler_ctx->upscaler->esrgan_upscaler == nullptr) {
+        return empty;
+    }
+
+    const int native_scale = get_upscale_factor(upscaler_ctx);
+    auto& evidence = upscaler_ctx->execution_evidence;
+    evidence = {};
+    evidence.version = SD_UPSCALER_EXECUTION_EVIDENCE_VERSION;
+    evidence.requested_scale = upscale_factor;
+    evidence.native_scale = native_scale > 0 ? static_cast<uint32_t>(native_scale) : 0;
+    evidence.source_width = input_image.width;
+    evidence.source_height = input_image.height;
+    evidence.tiled = upscaler_ctx->upscaler->tile_size > 0 &&
+                             (input_image.width > static_cast<uint32_t>(upscaler_ctx->upscaler->tile_size) ||
+                              input_image.height > static_cast<uint32_t>(upscaler_ctx->upscaler->tile_size))
+                         ? 1u
+                         : 0u;
+    if (native_scale <= 0 || upscale_factor != static_cast<uint32_t>(native_scale)) {
+        LOG_ERROR("requested upscale factor %u does not match model scale %d", upscale_factor, native_scale);
+        return empty;
+    }
+
+    UpscalerEvidenceScope evidence_scope(&evidence);
+    sd_image_t result = upscaler_ctx->upscaler->upscale(input_image, upscale_factor);
+    evidence.output_width = result.width;
+    evidence.output_height = result.height;
+    evidence.cancelled = sd_should_cancel() ? 1u : 0u;
+    evidence.completed = result.data != nullptr && evidence.cancelled == 0u &&
+                                 evidence.compute_invocation_count > 0u &&
+                                 evidence.compute_success_count == evidence.compute_invocation_count
+                             ? 1u
+                             : 0u;
+    return result;
+}
+
+extern "C" SD_API bool sd_get_upscaler_execution_evidence(
+    const upscaler_ctx_t* upscaler_ctx,
+    sd_upscaler_execution_evidence_t* evidence) {
+    if (upscaler_ctx == nullptr || evidence == nullptr ||
+        upscaler_ctx->execution_evidence.version != SD_UPSCALER_EXECUTION_EVIDENCE_VERSION) {
+        return false;
+    }
+    *evidence = upscaler_ctx->execution_evidence;
+    return true;
 }

 int get_upscale_factor(upscaler_ctx_t* upscaler_ctx) {
@@ -183,9 +287,12 @@ int get_upscale_factor(upscaler_ctx_t* upscaler_ctx) {
 }

 void free_upscaler_ctx(upscaler_ctx_t* upscaler_ctx) {
+    if (upscaler_ctx == nullptr) {
+        return;
+    }
     if (upscaler_ctx->upscaler != nullptr) {
         delete upscaler_ctx->upscaler;
         upscaler_ctx->upscaler = nullptr;
     }
-    free(upscaler_ctx);
+    delete upscaler_ctx;
 }
diff --git a/src/util.cpp b/src/util.cpp
index 77fc542..05399d3 100644
--- a/src/util.cpp
+++ b/src/util.cpp
@@ -340,6 +340,8 @@ int32_t sd_get_num_physical_cores() {

 static sd_progress_cb_t sd_progress_cb = nullptr;
 void* sd_progress_cb_data              = nullptr;
+static sd_cancel_cb_t sd_cancel_cb = nullptr;
+static void* sd_cancel_cb_data     = nullptr;

 static sd_preview_cb_t sd_preview_cb = nullptr;
 static void* sd_preview_cb_data      = nullptr;
@@ -613,6 +615,10 @@ void sd_set_progress_callback(sd_progress_cb_t cb, void* data) {
     sd_progress_cb      = cb;
     sd_progress_cb_data = data;
 }
+void sd_set_cancel_callback(sd_cancel_cb_t cb, void* data) {
+    sd_cancel_cb      = cb;
+    sd_cancel_cb_data = data;
+}
 void sd_set_preview_callback(sd_preview_cb_t cb, preview_t mode, int interval, bool denoised, bool noisy, void* data) {
     sd_preview_cb       = cb;
     sd_preview_cb_data  = data;
@@ -648,6 +654,9 @@ sd_progress_cb_t sd_get_progress_callback() {
 void* sd_get_progress_callback_data() {
     return sd_progress_cb_data;
 }
+bool sd_should_cancel() {
+    return sd_cancel_cb != nullptr && sd_cancel_cb(sd_cancel_cb_data);
+}

 sd_image_t tensor_to_sd_image(const sd::Tensor<float>& tensor, int frame_index) {
     const auto& shape = tensor.shape();
diff --git a/src/util.h b/src/util.h
index c3b06b1..e89d511 100644
--- a/src/util.h
+++ b/src/util.h
@@ -88,6 +88,7 @@ std::vector<std::pair<std::string, float>> split_quotation_attention(

 sd_progress_cb_t sd_get_progress_callback();
 void* sd_get_progress_callback_data();
+bool sd_should_cancel();

 sd_preview_cb_t sd_get_preview_callback();
 void* sd_get_preview_callback_data();
diff --git a/src/vae.hpp b/src/vae.hpp
index cc4cd96..9360146 100644
--- a/src/vae.hpp
+++ b/src/vae.hpp
@@ -8,6 +8,7 @@ struct VAE : public GGMLRunner {
 protected:
     SDVersion version;
     bool scale_input                                      = true;
+    sd_image_execution_evidence_t* image_execution_evidence = nullptr;
     virtual sd::Tensor<float> _compute(const int n_threads,
                                        const sd::Tensor<float>& z,
                                        bool decode_graph) = 0;
@@ -27,6 +28,89 @@ protected:
         }
     }

+    void record_tiling_invocation(bool decode_graph,
+                                  int small_width,
+                                  int small_height,
+                                  int tile_size_x,
+                                  int tile_size_y,
+                                  float tile_overlap,
+                                  bool circular_x,
+                                  bool circular_y) {
+        if (image_execution_evidence == nullptr) {
+            return;
+        }
+        int num_tiles_x = 0;
+        int num_tiles_y = 0;
+        float actual_overlap_x = 0.0f;
+        float actual_overlap_y = 0.0f;
+        sd_tiling_calc_tiles(num_tiles_x,
+                             actual_overlap_x,
+                             small_width,
+                             tile_size_x,
+                             tile_overlap,
+                             circular_x);
+        sd_tiling_calc_tiles(num_tiles_y,
+                             actual_overlap_y,
+                             small_height,
+                             tile_size_y,
+                             tile_overlap,
+                             circular_y);
+        const uint64_t planned_tile_count =
+            static_cast<uint64_t>(num_tiles_x) * static_cast<uint64_t>(num_tiles_y);
+        if (decode_graph) {
+            ++image_execution_evidence->vae_decode_tiling_invocation_count;
+            image_execution_evidence->vae_decode_planned_tile_count += planned_tile_count;
+            image_execution_evidence->vae_decode_tile_size_x =
+                static_cast<uint32_t>(tile_size_x);
+            image_execution_evidence->vae_decode_tile_size_y =
+                static_cast<uint32_t>(tile_size_y);
+            image_execution_evidence->vae_decode_tile_overlap_x = actual_overlap_x;
+            image_execution_evidence->vae_decode_tile_overlap_y = actual_overlap_y;
+        } else {
+            ++image_execution_evidence->vae_encode_tiling_invocation_count;
+            image_execution_evidence->vae_encode_planned_tile_count += planned_tile_count;
+            image_execution_evidence->vae_encode_tile_size_x =
+                static_cast<uint32_t>(tile_size_x);
+            image_execution_evidence->vae_encode_tile_size_y =
+                static_cast<uint32_t>(tile_size_y);
+            image_execution_evidence->vae_encode_tile_overlap_x = actual_overlap_x;
+            image_execution_evidence->vae_encode_tile_overlap_y = actual_overlap_y;
+        }
+    }
+
+    void record_tiling_success(bool decode_graph) {
+        if (image_execution_evidence == nullptr) {
+            return;
+        }
+        if (decode_graph) {
+            ++image_execution_evidence->vae_decode_tiling_success_count;
+        } else {
+            ++image_execution_evidence->vae_encode_tiling_success_count;
+        }
+    }
+
+    void record_tile_compute_attempt(bool decode_graph) {
+        if (image_execution_evidence == nullptr) {
+            return;
+        }
+        if (decode_graph) {
+            ++image_execution_evidence->vae_decode_tile_compute_attempt_count;
+        } else {
+            ++image_execution_evidence->vae_encode_tile_compute_attempt_count;
+        }
+    }
+
+    void record_tile_compute_success(bool decode_graph) {
+        if (image_execution_evidence == nullptr) {
+            return;
+        }
+        if (decode_graph) {
+            ++image_execution_evidence->vae_decode_tile_compute_success_count;
+        } else {
+            ++image_execution_evidence->vae_encode_tile_compute_success_count;
+        }
+    }
+
     sd::Tensor<float> tiled_compute(const sd::Tensor<float>& input,
                                     int n_threads,
                                     int output_width,
@@ -39,13 +123,20 @@ protected:
                                     bool circular_y,
                                     bool decode_graph,
                                     const char* error_message,
-                                    bool silent = false) {
+                                    bool silent                    = false,
+                                    bool record_execution_evidence = true) {
         auto on_processing = [&](const sd::Tensor<float>& input_tile) {
+            if (record_execution_evidence) {
+                record_tile_compute_attempt(decode_graph);
+            }
             auto output_tile = _compute(n_threads, input_tile, decode_graph);
             if (output_tile.empty()) {
                 LOG_ERROR("%s", error_message);
                 return sd::Tensor<float>();
             }
+            if (record_execution_evidence) {
+                record_tile_compute_success(decode_graph);
+            }
             return output_tile;
         };
         return ::process_tiles_2d(input,
@@ -65,6 +156,10 @@ public:
     VAE(SDVersion version, ggml_backend_t backend, ggml_backend_t params_backend)
         : version(version), GGMLRunner(backend, params_backend) {}

+    void set_image_execution_evidence(sd_image_execution_evidence_t* evidence) {
+        image_execution_evidence = evidence;
+    }
+
     int get_scale_factor() {
         int scale_factor = 8;
         if (version == VERSION_LTXAV) {
@@ -129,6 +224,14 @@ public:
             float tile_overlap;
             int tile_size_x, tile_size_y;
             get_tile_sizes(tile_size_x, tile_size_y, tile_overlap, tiling_params, W, H, 1.30539f);
+            record_tiling_invocation(false,
+                                     static_cast<int>(W),
+                                     static_cast<int>(H),
+                                     tile_size_x,
+                                     tile_size_y,
+                                     tile_overlap,
+                                     circular_x,
+                                     circular_y);
             LOG_DEBUG("VAE Tile size: %dx%d", tile_size_x, tile_size_y);
             output = tiled_compute(input,
                                    n_threads,
@@ -142,6 +245,9 @@ public:
                                    circular_y,
                                    false,
                                    "vae encode compute failed while processing a tile");
+            if (!output.empty()) {
+                record_tiling_success(false);
+            }
         } else {
             output = _compute(n_threads, input, false);
         }
@@ -163,7 +269,8 @@ public:
                              bool decode_video = false,
                              bool circular_x   = false,
                              bool circular_y   = false,
-                             bool silent       = false) {
+                             bool silent       = false,
+                             bool record_execution_evidence = true) {
         int64_t t0              = ggml_time_ms();
         sd::Tensor<float> input = x;
         sd::Tensor<float> output;
@@ -176,6 +283,16 @@ public:
             float tile_overlap;
             int tile_size_x, tile_size_y;
             get_tile_sizes(tile_size_x, tile_size_y, tile_overlap, tiling_params, input.shape()[0], input.shape()[1]);
+            if (record_execution_evidence) {
+                record_tiling_invocation(true,
+                                         static_cast<int>(input.shape()[0]),
+                                         static_cast<int>(input.shape()[1]),
+                                         tile_size_x,
+                                         tile_size_y,
+                                         tile_overlap,
+                                         circular_x,
+                                         circular_y);
+            }
             if (!silent) {
                 LOG_DEBUG("VAE Tile size: %dx%d", tile_size_x, tile_size_y);
             }
@@ -192,7 +309,11 @@ public:
                 circular_y,
                 true,
                 "vae decode compute failed while processing a tile",
-                silent);
+                silent,
+                record_execution_evidence);
+            if (!output.empty() && record_execution_evidence) {
+                record_tiling_success(true);
+            }
         } else {
             output = _compute(n_threads, input, true);
         }
